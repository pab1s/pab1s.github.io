---
import IsometricDiagram from "./IsometricDiagram.astro";
import SocialIconRow from "./SocialIconRow.astro";
import { siteConfig } from "../site-config";

const buildDate = new Date().toISOString().slice(0, 10).replace(/-/g, ".");
---

<section class="hero-section" aria-labelledby="hero-title">
  <div class="hero-grid">
    <div class="hero-title-block">
      <div class="hero-avatar-wrap">
        <img
          class="hero-avatar"
          src={siteConfig.avatarSrc}
          width="140"
          height="140"
          alt={`${siteConfig.author} portrait`}
          loading="eager"
          decoding="async"
        />
        <span class="hero-avatar-caption" aria-hidden="true">fig. a</span>
      </div>

      <div class="hero-headline">
        <h1 id="hero-title" class="hero-title">{siteConfig.author}</h1>
        <p class="hero-subtitle">{siteConfig.tagline}</p>
      </div>

      <p class="hero-desc">
        I turn raw data into <mark class="highlight-marker">insight</mark>, build things with PyTorch and curiosity,
        and cook up experiments in the lab. {siteConfig.currently}
      </p>

      <div class="hero-callouts">
        <span class="hero-callout">Loc. Madrid, ES</span>
        <span class="hero-callout" aria-hidden="true">·</span>
        <span class="hero-callout">Ver. 2.0</span>
        <span class="hero-callout" aria-hidden="true">·</span>
        <span class="hero-callout">Date {buildDate}</span>
        <span class="hero-callout" aria-hidden="true">·</span>
        <span class="hero-callout">Scale 1:1</span>
      </div>

      <SocialIconRow />
    </div>

    <div class="hero-drawing" aria-hidden="true">
      <IsometricDiagram />
    </div>
  </div>
</section>

<style>
  .hero-section {
    background: var(--canvas);
    border: 1px solid var(--hairline);
    padding: 64px;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2.5rem;
    align-items: center;
  }

  .hero-title-block {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .hero-avatar-wrap {
    position: relative;
    align-self: flex-start;
  }

  .hero-avatar {
    width: 140px;
    height: 140px;
    border: 1px solid var(--hairline);
    border-radius: var(--r-none);
    box-shadow: none;
    object-fit: cover;
    background: var(--canvas);
  }

  .hero-avatar-caption {
    position: absolute;
    bottom: -0.4rem;
    right: -0.4rem;
    font-family: var(--font-mono);
    font-size: 0.55rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--ink);
    background: var(--canvas);
    border: 1px solid var(--hairline);
    padding: 0.1rem 0.35rem;
    border-radius: var(--r-none);
  }

  .hero-headline {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .hero-title {
    font-family: var(--font-display);
    font-size: clamp(48px, 8vw, 88px);
    font-weight: 600;
    line-height: 0.909;
    letter-spacing: -1.76px;
    color: var(--ink);
    margin: 0;
    text-transform: none;
  }

  .hero-subtitle {
    font-family: var(--font-sans);
    font-size: 16px;
    color: var(--ink);
    margin: 0;
  }

  .hero-desc {
    font-family: var(--font-display);
    font-size: 24px;
    font-weight: 400;
    line-height: 1.333;
    color: var(--ink);
    max-width: 46ch;
    margin: 0.25rem 0 0;
  }

  .highlight-marker {
    background: var(--brand-green);
    color: #000000;
    padding: 0 4px;
  }

  .hero-callouts {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.35rem 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--ink-muted);
    letter-spacing: 0.04em;
  }

  .hero-callout {
    white-space: nowrap;
  }

  .hero-drawing {
    display: flex;
    align-items: center;
    justify-content: center;
    max-width: 380px;
    width: 100%;
    margin-left: 0;
  }

  @media (max-width: 860px) {
    .hero-section {
      padding: 32px;
    }

    .hero-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-drawing {
    margin-left: 1.5rem;
      margin-right: 0;
      max-width: 280px;
    }
  }

  @media (max-width: 480px) {
    .hero-section {
      padding: 24px;
    }

    .hero-title {
      font-size: 48px;
    }

    .hero-avatar {
      width: 120px;
      height: 120px;
    }
  }
</style>

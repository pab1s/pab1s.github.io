---
import { formatDate } from "../lib/date";

interface Props {
  slug: string;
  title: string;
  date: Date;
  description?: string;
  tags: string[];
  readTime?: number;
}

const { slug, title, date, description, tags, readTime } = Astro.props;

const dateStr = formatDate(date);
---

<div class="blog-post-row">
  <div class="blog-post-meta">
    <time class="blog-post-date" datetime={date.toISOString()}>
      {dateStr}
    </time>
    {readTime ? (
      <span class="blog-post-read-time" aria-label={`${readTime} minute read`}>
        {readTime} min
      </span>
    ) : null}
  </div>
  <div class="blog-post-main">
    <h2 class="blog-post-title">
      <a href={`/blog/${slug}/`}>{title}</a>
    </h2>
    {description?.trim() ? (
      <p class="blog-post-description">{description}</p>
    ) : null}
  </div>
</div>

<style>
  .blog-post-row {
    display: grid;
    grid-template-columns: 6.5rem 1fr;
    gap: var(--space-6);
    align-items: baseline;
    padding: 1.5rem 0;
    background: var(--canvas);
    border-radius: var(--r-none);
    box-shadow: none;
  }

  .blog-post-row:first-child {
    padding-top: 0.5rem;
  }

  .blog-post-row:hover .blog-post-title a {
    text-decoration: underline;
    text-decoration-color: var(--brand-green);
    text-decoration-thickness: 2px;
    text-underline-offset: 4px;
  }

  .blog-post-meta {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--ink-muted);
    letter-spacing: normal;
    flex-shrink: 0;
  }

  .blog-post-date {
    color: var(--ink-muted);
  }

  .blog-post-read-time {
    color: var(--ink-muted);
  }

  .blog-post-main {
    min-width: 0;
  }

  .blog-post-title {
    font-family: var(--font-display);
    font-size: 1.35rem;
    font-weight: 600;
    color: var(--ink);
    margin: 0;
    line-height: 1.25;
    letter-spacing: -0.02em;
  }

  .blog-post-title a {
    color: inherit;
    text-decoration: none;
  }

  .blog-post-description {
    font-family: var(--font-body);
    font-size: 1rem;
    color: var(--ink-muted);
    max-width: 60ch;
    line-height: 1.6;
    margin: var(--space-2) 0 0;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  @media (max-width: 640px) {
    .blog-post-row {
      grid-template-columns: 1fr;
      gap: var(--space-3);
      padding: var(--space-5) 0;
    }

    .blog-post-meta {
      flex-direction: row;
      align-items: baseline;
      gap: var(--space-3);
    }
  }
</style>

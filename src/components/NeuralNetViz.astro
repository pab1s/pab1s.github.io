---
---

<div class="nn-viz-mount">
  <canvas id="nn-viz-canvas" class="nn-viz-canvas" width="720" height="450" aria-hidden="true"
  ></canvas>
</div>

<script>
  import { attachNeuralNetViz } from "../lib/neural-net-viz";

  function boot() {
    const canvas = document.getElementById("nn-viz-canvas");
    if (!(canvas instanceof HTMLCanvasElement)) return;

    attachNeuralNetViz(canvas);
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", boot);
  else boot();
</script>

<style>
  .nn-viz-mount {
    position: relative;
    width: 100%;
    height: 100%;
    overflow: hidden;
  }

  .nn-viz-canvas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    display: block;
  }
</style>
